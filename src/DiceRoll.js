import React from 'react'

const DiceRoll = () => {
    
    // const sum = 0;
    // int count = 0;
    // while (sum != 7) {
    //         int die1 = r.nextInt(6) + 1; // between 1 to 6 inclusive
    //         int die2 = r.nextInt(6) + 1;
    //     sum = die1 + die2;
    //     System.out.println(die1 + " + " + die2 + " = " + sum);
    //     count++;
    // }
    // System.out.println("sum of 7 after " + count + " rolls");
    return (
        <>
            <div>DiceRoll</div>
        {/* button for dice roll */}
        </>

    )
}

export default DiceRoll